{
    "title": "Airfoil section referenced geometry",
    "description": "Represents sensor positions or surface nodes",
    "type": "object",
    "properties": {
        "xy_coordinates":{
            "title": "XY coordinates",
            "description": "Two arrays of XY coordinates of node locations, with aerofoil characteristic length normalised. If used for panel-code solver inputs these should be in a counterclockwise order from trailing edge. If used for sensor nodes, should be ordered by sensor number.",
            "type": "object",
            "properties": {
                "x_coordinates": {
                    "type": "array",
                    "items": {
                      "type": "number",
                      "minimum": -0.2,
                      "maximum": 1.2
                    }
                },
                "y_coordinates": {
                    "type": "array",
                    "items": {
                      "type": "number"
                    }
                }
            }
        },
        "radial_coordinates": {
            "description": "Defines the radial positions of the nodes in xy_coordinates",
            "oneOf": [
                {
                    "description": "A single value of r/r_tip determining the radial position of all x,y coordinates (e.g. on a cylinder)",
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1
                },
                {
                    "description": "An array of r/r_tip values the same size as `x_coordinates` and `y_coordinates` determining the radial position of each x,y coordinate (allowing non-cylindrical positioning)",
                    "type": "array",
                    "items": {
                      "type": "number",
                        "minimum": 0,
                        "maximum": 1
                    }
                }
            ]
        },
        "chord_length": {
            "description": "Chord length in m",
            "type": "number",
            "exclusiveMinimum": 0
        },
        "ordering": {
            "title": "Ordering of coordinate values",
            "description": "Either anticlockwise from the trailing edge (for panel solvers) or 'unknown' (represents some other ordering e.g. by sensor node index).",
            "type": "array",
            "items": {
                "type": "string",
                "enum": ["anticlockwise", "unknown"]
            }
        },
        "notes": {
            "description": "Add notes about where these coordinates came from",
            "type": "string"
        }
    }
}
